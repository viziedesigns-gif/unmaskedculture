# Project workflow

After every completed website or application update:

1. Verify the change in proportion to its risk.
2. Commit the completed change to the current branch with a concise message.
3. Push the commit to the configured GitHub upstream.
4. Deploy the pushed revision to the connected Hostinger website and verify that deployment completed successfully.

Never commit secrets, private configuration, runtime uploads, generated dependencies, or local build caches.
